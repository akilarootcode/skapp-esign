const styles = () => ({
  wrapper: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "flex-end",
    width: "100%",
    gap: "0.625rem"
  },
  containerStyles: {
    display: "flex",
    flexDirection: "row",
    gap: "0.625rem",
    alignItems: "center"
  }
});

export default styles;
