import { StyleProps } from "~community/common/types/CommonTypes";

export const styles = (): StyleProps => ({
  container: {
    width: "80%",
    maxWidth: "37.5rem"
  },
  workEmailStyle: { marginTop: "1.25rem" }
});
